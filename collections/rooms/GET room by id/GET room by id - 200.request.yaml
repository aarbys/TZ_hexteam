$kind: http-request
name: GET room by id - 200
url: "{{baseUrl}}/api/rooms/{{created_room}}"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 200\", function () {\r

      pm.response.to.have.status(200);\r

      })"
    language: text/javascript
order: 4344166977012187
